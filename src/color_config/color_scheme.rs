use raylib::prelude::*;

pub const PRIMARY_GREEN: Color = Color::new(34, 139, 34, 255); // Forest Green

// Secondary Green (lighter/darker for contrast)
pub const SECONDARY_GREEN: Color = Color::new(60, 179, 113, 255); // Medium Sea Green
pub const DARK_GREEN: Color = Color::new(0, 100, 0, 255); // Dark Green

// Background color (dark green for a soft, subtle background)
pub const BACKGROUND_GREEN: Color = Color::new(25, 50, 25, 255); // Dark Green Background

// Accent color (for buttons, highlights, etc.)
pub const ACCENT_GREEN: Color = Color::new(0, 255, 0, 255); // Lime Green for highlights

// Text color (white or light green for readability)
pub const TEXT_COLOR: Color = Color::new(255, 255, 255, 255); // White for text
